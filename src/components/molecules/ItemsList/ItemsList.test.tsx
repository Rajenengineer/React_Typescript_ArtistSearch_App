import ItemList from './ItemsList';

describe('ItemList Component', () => {
  it('should render ItemList', () => {

  });

  it('should render the given number of items', () => {

  });

  it('should render the return value of renderItem as the item', () => {

  });

  it('should render the loader when the list is loading', () => {

  });

  it('should render the message when list is empty', () => {

  });


});